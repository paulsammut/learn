#include <iostream>
#include <array>
#include <string>
#include <vector>
#include <math.h>

int main(void)
{
    printf("wtf");
    std::cout << "Wow this has been a while!" << std::endl;
    std::cout << sizeof(int) << std::endl;
    float num = pow(2, sizeof(int) * 8);
    std::cout << num << std::endl;
    return 0;
}

int doCRC_Check(int input)
{
    return 0;
}
